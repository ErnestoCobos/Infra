# Environment variables for Ansible
# Copy this file to .env and fill in the values
# Usage: source .env && ./scripts/run-ansible.sh

# DigitalOcean (for testing)
export SONAR_DO_IP=""

# Tailscale IPs (for production)
export SONAR_STAGING_IP=""
export SONAR_PRODUCTION_IP=""

# Ansible user
export ANSIBLE_USER="root"

# SonarQube Database
export SONARQUBE_DB_PASSWORD="ChangeMeNow!2024"

# SSH Key (optional, defaults to ~/.ssh/id_rsa)
# export ANSIBLE_PRIVATE_KEY_FILE="~/.ssh/id_rsa"
